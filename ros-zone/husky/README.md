TODO update the instructions for ROS Jade

The first time you need to execute the `setup.sh` script.

Then, to run the example run the following commands
`source devel/setup.bash`
`roslaunch husky.launch`
